# 1. 파이썬 가상환경 (절대 올리면 안 됨)
venv/
.venv/
env/

# 2. 컴파일러 캐시 파일
__pycache__/
*.pyc

# 3. 실시간으로 계속 쌓이는 분석용 결과 레포트 폴더
reports/

# 4. 다운로드된 원본 임시 엑셀 데이터 파일 (검증용 test.xls만 남기려면 별도 추가 가능)
*.xls
*.xlsx

# 제외 처리할 예외: test.xls는 깃허브 예제로 보존하고 싶을 경우
!test.xls

# 6. IDE 설정 파일
.vscode/
.idea/